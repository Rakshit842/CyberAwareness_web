# CyberMitra — Cyber Security Solutions

Clean React/Vite build based on the uploaded current website. Existing content and functionality are preserved; the duplicate logo wrapper and overly broad palette overrides were removed.

## Run
```bash
npm install
npm run dev
```
